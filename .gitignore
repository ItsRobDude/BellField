node_modules
.pnpm-store
.npm
coverage
release
*.log
.DS_Store

apps/**/node_modules
apps/**/.next
apps/**/dist
apps/**/.expo
apps/**/.turbo
apps/**/tsconfig.tsbuildinfo

packages/**/dist
packages/**/tsconfig.tsbuildinfo

# Local scratch: planning docs, AI-collaboration briefs, and generated transcripts.
# Kept on disk for reference but never committed (e.g. multi-thousand-line Codex logs).
artifacts/

# Local Claude Code preview tooling config (not shared project config).
.claude/launch.json
# Transient Codex review scratch files (prompt + captured output).
tmp-codex-*.txt
# Codex smoke-test artifacts: screenshots and generated documents from local runs.
.codex-smoke/

# Locally generated BellField-side license signing material and issuance ledgers.
license-keys/
tools/license/*.pem
tools/license/*.jsonl
# Locally generated BellField-side release signing material.
tools/update/*.pem

# wrangler local state
site/.wrangler/

# wrangler local state
.wrangler/
